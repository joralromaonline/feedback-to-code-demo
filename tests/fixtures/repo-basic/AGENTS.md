# Fixture repository instructions

- Keep the checkout submit label on one line.
- Make the smallest CSS change that fixes the reported overflow.
- Run the package scripts that are available before reporting success.
